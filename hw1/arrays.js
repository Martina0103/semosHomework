// Define an array with 7 members in a separate file and export it.
const array =['nule','eden','dva']; //definirame prazna niza, ne moze posle da ja menuvame 
let niza = []; //ne i inicijalizirame vrednosti na pocetok

//site se string, zatoa shto za zapishuvanje vo file mora da se string 

//dodeluvanje na 7 vrednosti
niza = ['el1', '5', 'niza', '54', '67', 'true', 'Martina'];

module.exports = {
    array,
    niza
}